#include <iostream>
#include<cmath>
#include<iomanip>


using namespace std;

int main() 
{

	int a;
	int b;
	int c;
	int delta;
	int pierw_z_Delty;
	int x1;
	int x2;
	

	cout << "Rownanie kwadratowe ma postac (a^2)x + bx + c " << endl;
	cout << "Podaj a: " << endl;
	cin >> a;
	cout << "Podaj b: " << endl;
	cin >> b;
	cout << "Podaj c: " << endl;
	cin >> c;

	cout << "Rownanie kwadratowe o podanych wspolczynnikach a, b i c ma postac: "; 
	cout << a; 
	cout << "x^2 + ";
	cout << b;
	cout << "x + ";
	cout << c;
	cout << "\n";

	cout << "---------------------------------------------------" << endl;

	delta = b*b - 4*a*c;
	

	pierw_z_Delty = sqrt(delta);
	

	if(delta > 0)
		{	
			x1 = (-b -pierw_z_Delty) / (2*a);
			x2 = (-b +pierw_z_Delty) * (2*a);

			cout << "Pierwsze miejsce zerowe tej funkcji to: ";
			cout << x1 << endl;
			cout << "---------------------------------------------------" << endl;
			cout << "Drugie miejsce zerowe tej funkcji to: ";
			cout << x2;
			
		}
	
		else
		{	
			x1 = (-b) / (2*a);
			cout << "Miejscem zerowym tej funkcji jest: ";
			cout << x1;
		}
	return 0;
	
}
